{
  "name": "react-webview-extension-monorepo",
  "private": true,
  "displayName": "react-webview-extension-monorepo",
  "description": "",
  "version": "0.0.1",
  "scripts": {
    "vscode": "npm run -C packages/vscode watch",
    "dev-vscode": "code --extensionDevelopmentPath=${workspaceFolder}/packages/vscode && npm run -C packages/vscode watch"
  },
  "devDependencies": {
    "react-webview-extension": "workspace:*",
    "react-webview-extension-web": "workspace:*"
  }
}